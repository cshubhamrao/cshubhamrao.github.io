{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{- /* site.Pages, not .Pages: in the home page's context .Pages excludes the home
         page itself, which silently kept "/" out of the sitemap. Taxonomy and term
         pages are filtered out — they are thin or empty listing pages and do not
         belong in a sitemap. */ -}}
  {{- $indexable := where site.Pages "Kind" "not in" (slice "taxonomy" "term") }}
  {{- range where $indexable "Sitemap.Disable" "ne" true }}
    {{- if .Permalink }}
  <url>
    <loc>{{ .Permalink }}</loc>{{ if not .Lastmod.IsZero }}
    <lastmod>{{ safeHTML ( .Lastmod.Format "2006-01-02T15:04:05-07:00" ) }}</lastmod>{{ end }}{{ with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>{{ end }}{{ if ge .Sitemap.Priority 0.0 }}
    <priority>{{ .Sitemap.Priority }}</priority>{{ end }}{{ if .IsTranslated }}{{ range .Translations }}
    <xhtml:link rel="alternate" hreflang="{{ .Language.Locale }}" href="{{ .Permalink }}" />{{ end }}
    <xhtml:link rel="alternate" hreflang="{{ .Language.Locale }}" href="{{ .Permalink }}" />{{ end }}
  </url>
    {{- end }}
  {{- end }}
  {{/* External pages not managed by Hugo */}}
  <url>
    <loc>{{ .Site.BaseURL }}WhatsFood/</loc>
    <lastmod>2026-01-19T00:00:00+00:00</lastmod>
  </url>
</urlset>
