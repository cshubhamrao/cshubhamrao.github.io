{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Site",
            "type": "shell",
            "command": "HUGO_ENV=production hugo --gc --minify -t KeepIt",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}